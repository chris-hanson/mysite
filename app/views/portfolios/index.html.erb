<h1>Portfolio</h1>

<%= link_to "Create new item", portfolios_path %>

<% @portfolio_items.each do |item| %>

  <%= link_to "Edit", edit_portfolio_path(item.id)%>


  <p><%= item.title %></p>
  <p><%= item.subtitle %></p>
  <%= image_tag item.thumb_image unless item.thumb_image.nil? %>

<% end %>